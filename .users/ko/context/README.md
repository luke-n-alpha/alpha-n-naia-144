# .users/ko/context — 사람용 한국어 mirror

본 디렉터리는 AI SoT(`/.agents/`)의 보조 mirror입니다.

- **SoT** = `/docs/` (방법론, 생성 계약, 라운드 기록) + `/.agents/context/` (JSON/YAML 운영 규칙)
- 본 디렉터리는 향후 필요 시 한국어 산출물 사본/요약을 보관.

현재는 placeholder. Phase A-2 진입 시 첫 사본을 채울 계획.
